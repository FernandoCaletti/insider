[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/Dockerfile.pipeline"

[deploy]
startCommand = "python -m pipeline.src.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1
cronSchedule = "0 3 */3 * *"
